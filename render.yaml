services:
  - type: web
    name: trend-predictor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Trendpredictorapp.py --server.port $PORT --server.address 0.0.0.0